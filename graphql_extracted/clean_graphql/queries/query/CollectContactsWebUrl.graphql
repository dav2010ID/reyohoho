query CollectContactsWebUrl(
  $language: LanguageISO639Scalar!
  $serviceName: String!
  $theme: String!
) {
  collectUserContacts(
    input: {
      language: $language
      widgetServiceName: $serviceName
      theme: $theme
    }
  ) {
    webViewUrl
    skipText
    alreadyCollected
  }
}
