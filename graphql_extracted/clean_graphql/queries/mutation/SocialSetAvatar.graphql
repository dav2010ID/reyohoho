mutation SocialSetAvatar($file: Upload!) {
  userProfile {
    social {
      setAvatar(avatarFile: $file) {
        status
        errors {
          __typename
          message
        }
      }
    }
  }
}
