mutation NotificationPreviewMarkViewedBatch($feedbackTokens: [String!]!) {
  notificationPreview {
    markViewedBatch(input: { feedbackTokens: $feedbackTokens }) {
      status
      error {
        message
      }
    }
  }
}
