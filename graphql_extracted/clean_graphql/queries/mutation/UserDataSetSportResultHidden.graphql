mutation UserDataSetSportResultHidden($value: Boolean!) {
  userProfile {
    sportContentSettings {
      setResultHidden(input: { value: $value }) {
        error {
          message
        }
        status
      }
    }
  }
}
