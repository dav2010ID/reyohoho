mutation RemoveMovieFavorite($movieId: Long!) {
  movie {
    favorite {
      remove(input: { movieId: $movieId }) {
        status
        error {
          message
          __typename
        }
        __typename
      }
      __typename
    }
    __typename
  }
}
