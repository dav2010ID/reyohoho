mutation MovieSetNotInterested($movieId: Long!) {
  movie {
    notInterested {
      set(input: { movieId: $movieId }) {
        error {
          message
          __typename
        }
        status
        __typename
      }
      __typename
    }
    __typename
  }
}
