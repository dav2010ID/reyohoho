mutation NotificationMarkViewed($feedbackToken: String!) {
  notification {
    markViewed(input: { feedbackToken: $feedbackToken }) {
      status
      error {
        message
        __typename
      }
      __typename
    }
    __typename
  }
}
