mutation SocialDismissRecommendation(
  $input: DismissSocialSubscriptionRecommendationInput!
) {
  userProfile {
    social {
      subscriptions {
        dismissRecommendation(input: $input) {
          status
          error {
            message
          }
        }
      }
    }
  }
}
