mutation DownloadSyncUpdateStatuses($input: DownloadsStatusUpdateInputInput!) {
  userProfile {
    downloads {
      update(input: $input) {
        status
        error {
          message
        }
      }
    }
  }
}
