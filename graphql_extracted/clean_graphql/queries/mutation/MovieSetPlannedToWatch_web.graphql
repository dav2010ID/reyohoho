mutation MovieSetPlannedToWatch($movieId: Long!) {
  movie {
    plannedToWatch {
      set(input: { movieId: $movieId }) {
        status
        error {
          message
          __typename
        }
        __typename
      }
      __typename
    }
    __typename
  }
}
