mutation SocialProfileUnsubscribeFromMe(
  $input: RemoveSocialSubscriptionFromMeInput!
) {
  userProfile {
    social {
      subscriptions {
        unsubscribeFromMe(input: $input) {
          status
          error {
            message
          }
        }
      }
    }
  }
}
