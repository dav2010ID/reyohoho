mutation SearchHistoryRemovePerson($personId: Long!) {
  emptyPrefixSuggest {
    history {
      remove {
        person(input: { id: $personId }) {
          __typename
          ...searchHistoryMutationResult
        }
      }
    }
  }
}

fragment searchHistoryMutationResult on EmptyPrefixSuggestHistoryPayload {
  status
  error {
    message
  }
}
