mutation SetPreferredWatchingLanguage($code: String!) {
  userProfile {
    localisation {
      setPreferredWatchingLanguage(code: $code) {
        error {
          message
        }
        status
      }
    }
  }
}
