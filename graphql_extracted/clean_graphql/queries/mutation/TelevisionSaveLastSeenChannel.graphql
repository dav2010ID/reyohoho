mutation TelevisionSaveLastSeenChannel($input: SaveLastSeenTvChannelInput!) {
  tvChannel {
    lastSeen {
      save(input: $input) {
        status
        error {
          message
        }
      }
    }
  }
}
