mutation UserAdultVerifyPinCode($input: PinCodeVerifyInput!) {
  userProfile {
    pinCode {
      verify(input: $input) {
        error {
          message
        }
        status
      }
    }
  }
}
